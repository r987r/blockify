#!/usr/bin/env bash
# Compile script for dma_axi32_core0_axim_resp testbench
# Generated by blockify generate_tb.py
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/../../.." && pwd)"
SLANG="${SLANG:-$ROOT_DIR/tools/bin/slang}"

# Verify slang exists
if [ ! -x "$SLANG" ]; then
    echo "ERROR: slang not found at $SLANG"
    echo "Run src/scripts/setup_slang.sh first"
    exit 1
fi

echo "Compiling: dma_axi32_core0_axim_resp testbench"
"$SLANG" \
  "$SCRIPT_DIR/../../../../import/dma_axi/src/dma_axi32/dma_axi32_core0_axim_resp.v" \
  "$SCRIPT_DIR/tb_dma_axi32_core0_axim_resp.v" \
  -I "$SCRIPT_DIR/../../../../import/dma_axi/src/dma_axi32" \
  -I "$SCRIPT_DIR/../../../../import/dma_axi/src/dma_axi32" \
  --allow-use-before-declare \
  --lint-only \
  --timescale "1ns/1ps"

echo "PASS: dma_axi32_core0_axim_resp testbench compiled successfully"